{
  "mcpServers": {
    "context7": {
      "command": "npx",
      "args": ["-y", "@upstash/context7-mcp@latest"],
      "env": {
        "CONTEXT7_DEFAULT_LIBRARIES": "express,mongoose,mongodb,chart.js,ejs"
      },
      "description": "Context7 MCP Server: Real-time official documentation and version-specific code patterns for Node.js, Express, MongoDB, Mongoose, and Chart.js."
    },
    "ponytail": {
      "command": "npx",
      "args": ["-y", "ponytail-mcp@latest"],
      "description": "Ponytail MCP Server: Enforces YAGNI, minimalist native web code, vanilla JavaScript patterns, and prevents forbidden external frontend frameworks."
    },
    "mongodb": {
      "command": "npx",
      "args": ["-y", "mongodb-mcp-server@latest"],
      "env": {
        "MONGODB_URI": "mongodb://127.0.0.1:27017/the_daily_web"
      },
      "description": "MongoDB MCP Server: Schema inspection, aggregation pipeline testing, database queries, and indexing validation for The Daily Web models."
    },
    "fetch": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-fetch@latest"],
      "description": "Fetch MCP Server: Testing RESTful API endpoints, verifying OpenWeatherMap API responses, and checking header rate-limits."
    },
    "github": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-github@latest"],
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": ""
      },
      "description": "GitHub MCP Server: Team Git workflow management, PR review tracking, branch creation, and student contribution logs."
    }
  }
}
